<%= form_for @search, :url=>{:controller=>'store', :action=>:index}  do |f| %>
 <div class="field">
    <%= f.label 'title' %><br />
    <%= f.text_field :title %>
  </div>
  	
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
